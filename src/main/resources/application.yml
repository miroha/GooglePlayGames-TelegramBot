bot:
  username: ${BOT_USERNAME}
  token: ${BOT_TOKEN}
  schedule:
    cron:
      update: 0 0 1 * * *
server:
  port: ${SERVER_PORT}
spring:
  data:
    mongodb:
      database: mongodb+srv://${DB_USERNAME}:${DB_PASSWORD}@telegram-bot.8dx8c.mongodb.net/${DB_NAME}?retryWrites=true&w=majority
